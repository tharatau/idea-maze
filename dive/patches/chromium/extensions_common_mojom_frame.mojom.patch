diff --git a/extensions/common/mojom/frame.mojom b/extensions/common/mojom/frame.mojom
index 081b0837ab..8a028cc5e0 100644
--- a/extensions/common/mojom/frame.mojom
+++ b/extensions/common/mojom/frame.mojom
@@ -26,6 +26,7 @@ struct ExecuteCodeParams {
   bool match_about_blank;
   // When to inject the code.
   RunLocation run_at;
+  bool in_main_world;
   // Whether the request is coming from a <webview>.
   bool is_web_view;
 };
@@ -131,6 +132,11 @@ interface LocalFrameHost {
       => (bool success,
           mojo_base.mojom.ListValue response_wrapper,
           string error);
+  [Sync]
+  RequestSync(RequestParams params)
+      => (bool success,
+          mojo_base.mojom.ListValue response_wrapper,
+          string error);

   // Notifies the browser process that a tab has started or stopped matching
   // certain conditions.  This method is called by response to several events:
